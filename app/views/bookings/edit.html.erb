<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
      <h1>Edit Your Stay at <%= @booking.tiny_home.name %></h1>
      <%= render 'form' %>
      <p class="my-2"><%= link_to 'Back', booking_path(@booking) %></p>
    </div>
  </div>
</div>
