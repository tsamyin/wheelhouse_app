<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
      <h1>Edit Your Stay at <%= @booking.tiny_home.name %></h1>
      <%= render 'form', tiny_home: @booking.tiny_home, booking: @booking %>|
      <%= link_to 'Back', tiny_homes_path %>
    </div>
  </div>
</div>
